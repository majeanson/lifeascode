{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Feature",
  "description": "A life-as-code feature document that captures the full lifecycle of a product/engineering feature, from problem statement through decisions, implementation, and annotations.",
  "type": "object",
  "required": ["featureKey", "title", "status", "problem"],
  "additionalProperties": false,
  "properties": {
    "featureKey": {
      "type": "string",
      "description": "Unique identifier for this feature. Auto-generated by lac-cli.",
      "pattern": "^[a-z][a-z0-9]*-\\d{4}-\\d{3}$",
      "examples": ["feat-2026-001", "proc-2026-001", "goal-2026-001"]
    },
    "title": {
      "type": "string",
      "description": "Short human-readable title for this feature.",
      "minLength": 1,
      "examples": ["User Authentication", "Dark Mode Support"]
    },
    "status": {
      "type": "string",
      "description": "Current lifecycle status of the feature.",
      "enum": ["draft", "active", "frozen", "deprecated"]
    },
    "problem": {
      "type": "string",
      "description": "The human problem this feature solves. Written from the user's perspective.",
      "minLength": 1,
      "examples": ["Users need a secure way to log in and manage their accounts."]
    },
    "analysis": {
      "type": "string",
      "description": "Free-form analysis of the problem space, research findings, or prior art."
    },
    "decisions": {
      "type": "array",
      "description": "Architectural or design decisions made during the development of this feature.",
      "items": {
        "$ref": "#/definitions/Decision"
      }
    },
    "implementation": {
      "type": "string",
      "description": "Implementation notes, approach summary, or links to relevant code."
    },
    "knownLimitations": {
      "type": "array",
      "description": "Known limitations, trade-offs, or tech debt introduced by this feature.",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "tags": {
      "type": "array",
      "description": "Free-form tags for search and categorisation.",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "examples": [["auth", "security", "backend"]]
    },
    "annotations": {
      "type": "array",
      "description": "Time-stamped annotations attached to this feature by team members.",
      "items": {
        "$ref": "#/definitions/Annotation"
      }
    },
    "schemaVersion": {
      "type": "integer",
      "description": "Schema version number for future migration tooling. When absent, tools assume version 1.",
      "minimum": 1,
      "examples": [1]
    },
    "owner": {
      "type": "string",
      "description": "The person or team responsible for this feature.",
      "examples": ["marc", "team-auth", "alice@example.com"]
    },
    "lineage": {
      "$ref": "#/definitions/Lineage"
    }
  },
  "definitions": {
    "Decision": {
      "type": "object",
      "required": ["decision", "rationale"],
      "additionalProperties": false,
      "properties": {
        "decision": {
          "type": "string",
          "description": "What was decided.",
          "minLength": 1
        },
        "rationale": {
          "type": "string",
          "description": "Why this decision was made.",
          "minLength": 1
        },
        "alternativesConsidered": {
          "type": "array",
          "description": "Other options that were evaluated but not chosen.",
          "items": {
            "type": "string"
          }
        },
        "date": {
          "type": "string",
          "description": "ISO 8601 date when this decision was recorded (YYYY-MM-DD).",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "examples": ["2026-03-15"]
        }
      }
    },
    "Annotation": {
      "type": "object",
      "required": ["id", "author", "date", "type", "body"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for this annotation.",
          "minLength": 1
        },
        "author": {
          "type": "string",
          "description": "Name or identifier of the person who wrote this annotation.",
          "minLength": 1
        },
        "date": {
          "type": "string",
          "description": "ISO 8601 date string when this annotation was created.",
          "examples": ["2026-03-15"]
        },
        "type": {
          "type": "string",
          "description": "Category of annotation.",
          "enum": ["decision", "warning", "assumption", "lesson", "breaking-change", "question", "tech-debt"]
        },
        "body": {
          "type": "string",
          "description": "The annotation content.",
          "minLength": 1
        }
      }
    },
    "Lineage": {
      "type": "object",
      "description": "Parent/child relationships between features (e.g. spawned sub-features).",
      "additionalProperties": false,
      "properties": {
        "parent": {
          "oneOf": [
            {
              "type": "string",
              "pattern": "^[a-z][a-z0-9]*-\\d{4}-\\d{3}$",
              "description": "featureKey of the parent feature."
            },
            { "type": "null" }
          ]
        },
        "children": {
          "type": "array",
          "description": "featureKeys of child features spawned from this one.",
          "items": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9]*-\\d{4}-\\d{3}$"
          }
        },
        "spawnReason": {
          "oneOf": [
            {
              "type": "string",
              "description": "Why this feature was spawned from its parent."
            },
            { "type": "null" }
          ]
        }
      }
    }
  }
}
